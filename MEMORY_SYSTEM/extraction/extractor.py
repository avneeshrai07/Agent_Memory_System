# MEMORY_SYSTEM/extraction/extractor.py

async def extract_facts(user_message: str, agent_response: str) -> list:
    """
    Call Bedrock (or any LLM) to extract facts.
    Must return structured ExtractedFact objects.
    """
    # You already have Bedrock code — reuse it here
    raise NotImplementedError
